import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'toldos.premium',
  appName: 'toldos-premium',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
